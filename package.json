{
  "name": "quant-question-bank",
  "private": true,
  "description": "Static desk of later-round quant interview problems.",
  "scripts": {
    "validate": "node scripts/validate-catalog.mjs",
    "test": "node --test tests/schema.test.mjs && playwright test",
    "test:schema": "node --test tests/schema.test.mjs",
    "test:browser": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
